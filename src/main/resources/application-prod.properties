# Production profile — override via env vars or --spring.datasource.*
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/table_name_of_your_database}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
